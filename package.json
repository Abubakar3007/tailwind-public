{
  "name": "tailwind-public",
  "version": "1.0.0",
  "scripts": {
    "build": "npx tailwindcss -i ./input.css -o ./assets/tailwind.min.css --minify"
  },
  "devDependencies": {
    "tailwindcss": "^3.4.1"
  }
}